# Backend environment configuration
# Copy this file to config/backend.env and fill in your values.
# config/backend.env is gitignored and must NEVER be committed.

# OpenAI-compatible API key used by the backend modules (CoT, Pipeline, Refine)
API_KEY=your_api_key_here
