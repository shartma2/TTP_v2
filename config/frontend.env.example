# Frontend environment configuration
# Copy this file to config/frontend.env and fill in your values.
# config/frontend.env is gitignored and must NEVER be committed.

# URL of the backend service as seen from the frontend container
# Default when using docker-compose: http://backend:8000
BACKEND_URL=http://backend:8000
