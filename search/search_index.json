{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Text-to-PASS Generator (TTP_v2)","text":"<p>This project is a bachelor thesis at the University of M\u00fcnster, created by Simon Hartmann (CC2026).</p> <p>The goal of TTP_v2 is to transform natural-language process descriptions into structured PASS artifacts and provide an end-to-end workflow for generation, validation, refinement, and export.</p>"},{"location":"#container-architecture","title":"Container architecture","text":"<p>The system is designed as two Docker containers:</p> <ul> <li>Backend container (FastAPI): asynchronous job execution, module orchestration, validation/repair logic, and artifact logging.</li> <li>Frontend container (Next.js): user interface for creating jobs, monitoring status, viewing results, refining models, and downloading exports.</li> </ul>"},{"location":"#core-capabilities","title":"Core capabilities","text":"<ul> <li>Chain-of-Thought generation for text-based reasoning outputs.</li> <li>Pipeline execution with staged <code>generate -&gt; validate -&gt; repair</code> flow for PASS model processing.</li> <li>Human-in-the-loop refinement based on previously generated jobs.</li> <li>Export module for producing results in multiple formats (e.g., <code>.json</code>, <code>.txt</code>, <code>.owl</code>).</li> <li>Asynchronous job handling with status tracking (<code>queued</code>, <code>running</code>, <code>done</code>, <code>failed</code>).</li> </ul>"},{"location":"#license","title":"License","text":"<p>This project is released under the MIT License.</p>"},{"location":"#disclaimer","title":"Disclaimer","text":"<p>Large parts of this documentation were created with AI agent support. While reviewed, it may still contain mistakes or outdated details.</p> <p>If you find an error, please open an issue or report it in the discussions panel.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This section explains how to set up and run the Text-to-PASS Generator locally using Docker.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<p>The only requirement to run the application is a working installation of Docker Engine. If you are using Docker Desktop, it already includes Docker Engine and is fully sufficient.</p> <p>Please follow the official installation guide for your operating system: \ud83d\udc49 https://docs.docker.com/engine/install/</p> <p>Make sure Docker is installed and running before proceeding.</p>"},{"location":"getting-started/#environment-configuration","title":"Environment Configuration","text":"<p>Before starting the application, the required environment variables must be configured.</p> <p>Example configuration files are provided in the <code>/config</code> directory:</p> <ul> <li><code>backend.env.example</code></li> <li><code>frontend.env.example</code></li> </ul> <p>Copy these files and rename them to: config/backend.env config/frontend.env</p> <p>Then fill in the required values (e.g., API keys).</p> <p>\ud83d\udc49 See the Configuration section of the documentation for more details.</p>"},{"location":"getting-started/#starting-the-application","title":"Starting the Application","text":"<p>To start the application, navigate to the project root directory and run:</p> <pre><code>docker compose up --build\n</code></pre> <p>This command:</p> <ul> <li>Builds the Docker images (if necessary)</li> <li>Starts all defined services (backend and frontend)</li> </ul>"},{"location":"getting-started/#when-to-use-build","title":"When to use <code>--build</code>","text":"<p>Use the <code>--build</code> flag when:</p> <ul> <li>Running the project for the first time</li> <li>Changes were made to source code or dependencies</li> </ul> <p>Otherwise, you can start the containers without rebuilding: <pre><code>docker compose up\n</code></pre></p>"},{"location":"getting-started/#running-in-detached-mode","title":"Running in detached mode","text":"<p>To run the containers in the background, use: <pre><code>docker compose up -d\n</code></pre></p> <p>This is useful when you do not need to monitor logs in the terminal.</p>"},{"location":"getting-started/#accessing-the-application","title":"Accessing the Application","text":"<p>Once the containers are running:</p> <ul> <li> <p>The frontend (web interface) is available at: http://localhost:3001</p> </li> <li> <p>The backend API runs internally on port <code>8000</code> and is accessed by the frontend via Docker networking.</p> </li> </ul>"},{"location":"getting-started/#running-containers","title":"Running Containers","text":"<p>The following containers will be active:</p> <ul> <li> <p>backend   FastAPI service responsible for job execution and module processing</p> </li> <li> <p>frontend   Next.js application providing the user interface</p> </li> </ul> <p>To verify running containers, use:</p> <pre><code>docker ps\n</code></pre> <p>This setup provides a fully containerized environment and requires no additional local dependencies.</p>"},{"location":"repo-structure/","title":"Repository Structure","text":"<p>This section provides a brief overview of the project structure of the Text-to-PASS Generator.</p>"},{"location":"repo-structure/#overview","title":"Overview","text":"<p>The repository is organized into four main directories: - backend - frontend - config - docs - docker-compose.yaml - mkdocs.yml - README.md</p>"},{"location":"repo-structure/#backend","title":"Backend","text":"<p>The <code>backend</code> directory contains the FastAPI application that implements the core logic of the system.</p> <p>Key components include:</p> <ul> <li> <p>API Layer (<code>/api</code>)   Defines REST endpoints (e.g., job creation and status retrieval)</p> </li> <li> <p>Application Layer (<code>/app</code>)   Contains core services such as the job service, module registry, and utility functions</p> </li> <li> <p>Modules (<code>/modules</code>)   Implements the main functionality:</p> </li> <li>CoT (Chain-of-Thought generation)</li> <li>Pipeline (generation, validation, repair)</li> <li>Refine (human-in-the-loop improvements)</li> <li>Export (format conversion)</li> <li> <p>Rendering (diagram generation)</p> </li> <li> <p>Logs (<code>/logs</code>)   Stores generated artifacts and runtime logs</p> </li> </ul>"},{"location":"repo-structure/#frontend","title":"Frontend","text":"<p>The <code>frontend</code> directory contains the Next.js application that provides the user interface.</p> <p>Key parts:</p> <ul> <li> <p>App Router (<code>/app</code>)   Main application structure including pages and layout</p> </li> <li> <p>API Routes (<code>/app/api</code>)   Proxy layer forwarding requests to the backend</p> </li> <li> <p>Modules (<code>/app/modules</code>)   UI components representing backend functionality (e.g., pipeline, refine, export)</p> </li> <li> <p>Utilities (<code>/app/util</code>)   Helper functions (e.g., job polling, rendering results)</p> </li> </ul>"},{"location":"repo-structure/#config","title":"Config","text":"<p>The <code>config</code> directory contains environment configuration templates:</p> <ul> <li><code>backend.env.example</code> \u2013 Backend configuration (e.g., API keys)</li> <li><code>frontend.env.example</code> \u2013 Frontend configuration (e.g., backend URL)</li> </ul> <p>These files must be copied and adapted before running the system.</p>"},{"location":"repo-structure/#docs","title":"Docs","text":"<p>The <code>docs</code> directory contains the MkDocs documentation files.</p> <p>The documentation structure is defined in <code>mkdocs.yml</code>.</p>"},{"location":"repo-structure/#infrastructure-files","title":"Infrastructure Files","text":"<ul> <li> <p><code>docker-compose.yaml</code>   Defines and orchestrates the backend and frontend services</p> </li> <li> <p><code>mkdocs.yml</code>   Configuration file for generating the documentation site</p> </li> </ul>"},{"location":"repo-structure/#summary","title":"Summary","text":"<p>The project follows a clear separation of concerns:</p> <ul> <li>Backend \u2192 processing logic and LLM integration  </li> <li>Frontend \u2192 user interface and interaction  </li> <li>Config \u2192 environment-specific settings  </li> <li>Docs \u2192 structured project documentation  </li> </ul> <p>This modular structure supports extensibility, maintainability, and clear system boundaries.</p>"},{"location":"backend/api/","title":"API Layer","text":"<p>The API layer exposes the backend functionality via HTTP endpoints implemented with FastAPI. All routes are mounted under the global <code>/api</code> prefix (configured in main.py).</p> <p>The API is intentionally thin: it performs request validation, delegates execution to the service layer, and maps domain errors to HTTP responses. No business logic is implemented directly inside the route handlers.</p>"},{"location":"backend/api/#router-composition","title":"Router Composition","text":"<p>The central router aggregates all sub-routers and defines their URL prefixes.</p> <p>File: <code>api/router.py</code></p> <pre><code>from fastapi import APIRouter\nfrom api.routes.health import router as health_router\nfrom api.routes.jobs import router as jobs_router\n\napi_router = APIRouter()\napi_router.include_router(health_router, prefix=\"/health\", tags=[\"health\"])\napi_router.include_router(jobs_router, prefix=\"/jobs\", tags=[\"jobs\"])\n</code></pre>"},{"location":"backend/api/#endpoint-overview","title":"Endpoint Overview","text":"<p>The API currently exposes two main endpoint groups with clearly separated responsibilities:</p>"},{"location":"backend/api/#health-endpoints","title":"Health Endpoints","text":"<p>Purpose: System monitoring and availability checks Typical use: Container health checks, uptime verification, orchestration tools</p> <p>The health endpoint provides a lightweight response indicating whether the backend service is running. It does not depend on business logic or internal state.</p>"},{"location":"backend/api/#job-endpoints","title":"Job Endpoints","text":"<p>Purpose: Core interaction with the backend system Typical use: Creating, monitoring, and retrieving processing tasks</p> <p>The job endpoints act as the primary interface for clients. They accept requests, delegate execution to the service layer, and return job-related data such as status and results.</p>"},{"location":"backend/main/","title":"Backend Application Entry Point (<code>main.py</code>)","text":"<p>The <code>main.py</code> file is the entry point of the backend service. It is responsible for initializing and configuring the FastAPI application instance, as well as managing its lifecycle.</p> <p>Its responsibilities are strictly limited to:</p> <ul> <li>Defining application startup and shutdown behavior</li> <li>Configuring logging</li> <li>Initializing shared services</li> <li>Registering middleware (CORS)</li> <li>Mounting the API router</li> </ul> <p>This file acts as the composition root, where core components are instantiated and wired together.</p>"},{"location":"backend/main/#lifecycle-management","title":"Lifecycle Management","text":"<p>The application lifecycle is managed using FastAPI\u2019s lifespan mechanism via an <code>@asynccontextmanager</code>:</p> <pre><code>@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    configure_logging(logging.INFO)\n    logger = get_logger(\"backend.main\")\n    logger.info(\"Starting up application...\")\n\n    app.state.job_service = JobService(max_concurrent_jobs=4)\n    logger.info(\"JobService initialized.\")\n\n    yield\n\n    logger.info(\"Backend shutdown initiated\")\n    await app.state.job_service.shutdown()\n    logger.info(\"Backend shutdown complete\")\n</code></pre>"},{"location":"backend/main/#startup-phase","title":"Startup Phase","text":"<p>Before the <code>yield</code>, the application performs initialization:</p> <ul> <li>Logging is configured via <code>configure_logging</code></li> <li>A module-specific logger is created</li> <li>The <code>JobService</code> is instantiated with a defined concurrency limit</li> <li>The service is attached to <code>app.state</code> for global access</li> </ul> <p>This ensures that all required runtime components are available before handling requests.</p>"},{"location":"backend/main/#shutdown-phase","title":"Shutdown Phase","text":"<p>After the yield, graceful shutdown logic is executed:</p> <ul> <li>A shutdown log message is emitted</li> <li>The <code>JobService</code> is properly terminated via its <code>shutdown()</code> method</li> <li>Final shutdown confirmation is logged</li> </ul> <p>This guarantees a clean termination of background processes and resources.</p>"},{"location":"backend/main/#middleware-configuration-cors","title":"Middleware Configuration (CORS)","text":"<p>CORS middleware is configured to allow requests from the frontend:</p> <pre><code>app.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\n        \"http://localhost:3001\",\n    ],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n</code></pre> <p>Key aspects:</p> <ul> <li>Restricts access to the local frontend origin</li> <li>Allows credentials and all HTTP methods/headers</li> <li>Enables browser-based communication between frontend and backend</li> </ul>"},{"location":"backend/main/#api-router-registration","title":"API Router Registration","text":"<p>The API routes are registered with a common prefix:</p> <pre><code>app.include_router(api_router, prefix=\"/api\")\n</code></pre> <p>This ensures that all endpoints are accessible under the <code>/api</code> namespace.</p>"},{"location":"backend/overview/","title":"Backend Overview","text":"<p>The backend of the Text-to-PASS Generator is implemented as a FastAPI application and provides the core functionality of the system. It is responsible for processing user requests, executing modules, and managing asynchronous jobs.</p>"},{"location":"backend/overview/#purpose","title":"Purpose","text":"<p>The backend acts as the central processing unit of the application. Its main responsibilities include:</p> <ul> <li>Accepting and handling API requests</li> <li>Managing asynchronous job execution</li> <li>Coordinating module execution</li> <li>Interacting with external services (e.g., LLM APIs)</li> <li>Returning structured results to the frontend</li> </ul>"},{"location":"backend/overview/#architectural-overview","title":"Architectural Overview","text":"<p>The backend follows a modular and layered architecture: - api/ - app/ - modules/ - logs/</p>"},{"location":"backend/overview/#key-layers","title":"Key Layers","text":"<ul> <li> <p>API Layer (<code>/api</code>)   Defines REST endpoints and handles incoming HTTP requests</p> </li> <li> <p>Application Layer (<code>/app</code>)   Contains core services such as:</p> </li> <li>Job management</li> <li>Module registry</li> <li> <p>Shared utilities (logging, exceptions)</p> </li> <li> <p>Module Layer (<code>/modules</code>)   Implements the domain-specific logic of the system. Each module encapsulates a specific task (e.g., generation, validation, refinement).</p> </li> <li> <p>Logging (<code>/logs</code>)   Stores runtime logs and generated artifacts</p> </li> </ul>"},{"location":"backend/app/registry/","title":"App Registry Layer (<code>app/registry/modules.py</code>)","text":"<p>The registry layer is the backend\u2019s module dispatch table. It maps external module names (received through the API) to concrete Python callables that execute the module logic.</p> <p>In this project, the <code>JobService</code> does not import every module directly. Instead, it resolves modules through this registry, which keeps job orchestration decoupled from module implementations.</p>"},{"location":"backend/app/registry/#responsibility","title":"Responsibility","text":"<p>The registry has one focused responsibility:</p> <ul> <li>Define the set of callable backend modules</li> <li>Provide a stable key-based lookup for job execution (<code>\"cot\"</code>, <code>\"pipeline\"</code>, <code>\"export\"</code>, ...)</li> <li>Enforce a shared runtime contract: each module exposes a <code>run(payload)</code> entrypoint</li> </ul> <p>This design keeps the orchestration path simple and explicit.</p>"},{"location":"backend/app/registry/#current-structure","title":"Current structure","text":"<pre><code>from modules.cot.main import run as run_cot\nfrom modules.pipeline.main import run as run_pipeline\n...\n\nModuleFn = Callable[[dict[str, Any] | None], Any]\n\nMODULES: dict[str, ModuleFn] = {\n    \"cot\": run_cot,\n    \"pipeline\": run_pipeline,\n    \"export\": run_export,\n    \"refine\": run_refine,\n    \"rendering\": run_rendering,\n}\n</code></pre> <p>Technically, modules can be synchronous or asynchronous. <code>JobService</code> handles both modes at runtime.</p>"},{"location":"backend/app/registry/#runtime-interaction","title":"Runtime interaction","text":"<p>At runtime, the flow is:</p> <ol> <li>API receives <code>POST /api/jobs</code> with <code>module</code> and optional <code>payload</code></li> <li><code>JobService.create_job(...)</code> normalizes module name (<code>strip().lower()</code>)</li> <li>Module key is validated against <code>MODULES</code></li> <li><code>_run_job(...)</code> fetches the function from the registry and executes it</li> </ol> <p>This means the registry is the single source of truth for what can be executed via the jobs API.</p>"},{"location":"backend/app/registry/#extension-workflow-add-a-new-module","title":"Extension workflow: add a new module","text":"<p>To make a new module executable through the API:</p> <ol> <li>Implement a <code>run(payload: dict[str, Any] | None)</code> function in the module package</li> <li>Import that <code>run</code> function in <code>app/registry/modules.py</code></li> <li>Register it in the <code>MODULES</code> dictionary under a unique key</li> </ol> <p>After that, clients can submit jobs with the new <code>module</code> key.</p>"},{"location":"backend/app/registry/#why-this-matters","title":"Why this matters","text":"<ul> <li>Keeps orchestration logic (<code>JobService</code>) independent from module internals</li> <li>Makes supported modules explicit and auditable</li> <li>Reduces accidental coupling and dynamic import complexity</li> <li>Provides one clear place to review execution surface of the backend</li> </ul>"},{"location":"backend/app/services/","title":"App Services Layer (<code>app/services/job_service.py</code>)","text":"<p>The services layer contains backend business orchestration. In the current architecture, this layer is centered around <code>JobService</code>, which manages asynchronous module execution and in-memory job state.</p> <p>The API layer delegates job operations to this service instead of executing module logic directly in route handlers.</p>"},{"location":"backend/app/services/#core-responsibilities-of-jobservice","title":"Core responsibilities of <code>JobService</code>","text":"<p><code>JobService</code> is responsible for:</p> <ul> <li>Creating and tracking jobs (<code>queued</code>, <code>running</code>, <code>done</code>, <code>failed</code>)</li> <li>Validating requested module names against the module registry</li> <li>Executing module functions in the background</li> <li>Limiting concurrent execution via an <code>asyncio.Semaphore</code></li> <li>Capturing module results or controlled/uncontrolled errors</li> <li>Supporting graceful shutdown by cancelling pending tasks</li> </ul> <p>This makes <code>JobService</code> the central runtime coordinator for backend workloads.</p>"},{"location":"backend/app/services/#data-model-jobrecord","title":"Data model: <code>JobRecord</code>","text":"<p>Each job is represented by a <code>JobRecord</code> dataclass containing:</p> <ul> <li>status</li> <li>module</li> <li>result</li> <li>error</li> <li>created/start/finish timestamps</li> </ul> <p>The service stores these records in an in-memory dictionary keyed by <code>job_id</code>.</p> <p>Important implication: job state is process-local and non-persistent. A restart clears all existing job records.</p>"},{"location":"backend/app/services/#lifecycle-of-a-job","title":"Lifecycle of a job","text":""},{"location":"backend/app/services/#1-job-creation","title":"1) Job creation","text":"<p><code>create_job(module, payload)</code> performs:</p> <ul> <li>module normalization (<code>strip().lower()</code>)</li> <li>module existence check against <code>MODULES</code></li> <li><code>job_id</code> generation (<code>uuid4</code>)</li> <li>payload enrichment with <code>job_id</code></li> <li>in-memory insertion with status <code>queued</code></li> <li>scheduling <code>_run_job(...)</code> via <code>asyncio.create_task(...)</code></li> </ul> <p>The method returns immediately with <code>job_id</code>, enabling non-blocking API behavior.</p>"},{"location":"backend/app/services/#2-job-execution","title":"2) Job execution","text":"<p><code>_run_job(job_id, module, payload)</code> runs inside semaphore-controlled context:</p> <ul> <li>sets status to <code>running</code></li> <li>resolves callable from registry</li> <li>executes async modules via <code>await</code> or sync modules via <code>asyncio.to_thread(...)</code></li> <li>writes result and marks status <code>done</code></li> </ul>"},{"location":"backend/app/services/#3-error-handling","title":"3) Error handling","text":"<p>Two error paths are used:</p> <ul> <li><code>JobError</code>: controlled/domain failure, mapped to a user-facing error string</li> <li>generic <code>Exception</code>: unexpected runtime failure, logged with stack trace</li> </ul> <p>Both paths mark status <code>failed</code> and set <code>finished_at</code>.</p>"},{"location":"backend/app/services/#api-integration","title":"API integration","text":"<p><code>app/deps.py</code> exposes <code>get_job_service(request)</code> to fetch the singleton service from <code>app.state</code>.</p> <p>This dependency is injected into route handlers in <code>api/routes/jobs.py</code>:</p> <ul> <li><code>POST /api/jobs</code> \u2192 <code>create_job(...)</code></li> <li><code>GET /api/jobs</code> \u2192 <code>get_all_jobs()</code></li> <li><code>GET /api/jobs/{job_id}</code> \u2192 <code>get_job(...)</code></li> </ul> <p>As a result, API endpoints remain thin, while service logic stays centralized and testable.</p>"},{"location":"backend/app/services/#startup-and-shutdown-integration","title":"Startup and shutdown integration","text":"<p><code>main.py</code> initializes one <code>JobService</code> instance during lifespan startup and stores it in <code>app.state.job_service</code>.</p> <p>During shutdown, <code>JobService.shutdown()</code> cancels tracked tasks and awaits them with <code>return_exceptions=True</code>, allowing clean service termination.</p>"},{"location":"backend/app/services/#design-notes-and-trade-offs","title":"Design notes and trade-offs","text":"<ul> <li>Strength: very simple async orchestration model</li> <li>Strength: clear separation between API transport and job execution logic</li> <li>Trade-off: in-memory storage means no durability across restarts</li> <li>Trade-off: no built-in retry, priority, or distributed execution (by design, for now)</li> </ul>"},{"location":"backend/app/utils/","title":"App Utilities Layer (<code>app/utils</code>)","text":"<p>The utilities layer provides cross-cutting helpers used by services and modules. In this backend, utilities focus on three concerns:</p> <ul> <li>structured logging</li> <li>serialization to JSON-safe structures</li> <li>domain-specific exception types</li> </ul> <p>These helpers keep module/service code focused on business logic.</p>"},{"location":"backend/app/utils/#logging-utilities-apputilsloggingpy","title":"Logging utilities (<code>app/utils/logging.py</code>)","text":""},{"location":"backend/app/utils/#configure_logging","title":"<code>configure_logging(...)</code>","text":"<p>Initializes root logging with a JSON-style formatter and a default field filter.</p> <p>The <code>_DefaultFieldsFilter</code> ensures common metadata is present on each log record:</p> <ul> <li><code>timestamp</code></li> <li><code>job_id</code> (defaults to <code>\"N/A\"</code> if absent)</li> </ul> <p>Example output shape:</p> <pre><code>{\"timestamp\":\"...\",\"level\":\"INFO\",\"job_id\":\"...\",\"message\":\"...\"}\n</code></pre> <p>This makes logs machine-readable and easy to correlate per job.</p>"},{"location":"backend/app/utils/#get_loggername","title":"<code>get_logger(name)</code>","text":"<p>Returns namespaced loggers used across backend modules/services (e.g. <code>services.job_service</code>, <code>modules.pipeline.main</code>).</p>"},{"location":"backend/app/utils/#save_artifactoutput-input-job_id-prefix","title":"<code>save_artifact(output, input, job_id, prefix)</code>","text":"<p>Persists execution artifacts as JSON files in <code>/app/backend/logs</code>.</p> <p>It stores:</p> <ul> <li>job metadata (<code>job_id</code>, timestamp)</li> <li>normalized input</li> <li>normalized output</li> </ul> <p>This is especially useful for debugging pipeline stages and preserving intermediate outputs.</p>"},{"location":"backend/app/utils/#serialization-utility-apputilsjsonablepy","title":"Serialization utility (<code>app/utils/jsonable.py</code>)","text":""},{"location":"backend/app/utils/#to_jsonableobj","title":"<code>to_jsonable(obj)</code>","text":"<p>Converts arbitrary Python objects into JSON-serializable data recursively.</p> <p>Handled types include:</p> <ul> <li>primitives (<code>str</code>, <code>int</code>, <code>float</code>, <code>bool</code>, <code>None</code>)</li> <li><code>datetime</code> \u2192 ISO string</li> <li><code>Enum</code> \u2192 enum value</li> <li><code>Path</code> \u2192 string path</li> <li><code>bytes</code> \u2192 UTF-8 text with replacement fallback</li> <li><code>dict</code> / <code>list</code> / <code>tuple</code> / <code>set</code></li> <li>Pydantic models (<code>model_dump</code> / <code>dict</code>)</li> </ul> <p>As a final fallback, it returns <code>repr(obj)</code> to guarantee log/artifact safety.</p> <p>This function is the key reason artifact logging can accept diverse model outputs without failing JSON encoding.</p>"},{"location":"backend/app/utils/#exception-taxonomy-apputilsexceptionspy","title":"Exception taxonomy (<code>app/utils/exceptions.py</code>)","text":"<p>The backend defines a small domain exception hierarchy:</p> <ul> <li><code>JobError</code> (base class for controlled job failures)</li> <li><code>MissingParameterException</code></li> <li><code>ModelValidationException</code></li> <li><code>InvalidPASSModelException</code></li> <li><code>InvalidExportFormatException</code></li> <li><code>JobNotFoundException</code></li> </ul> <p><code>JobService</code> treats <code>JobError</code> differently from unexpected exceptions:</p> <ul> <li><code>JobError</code> \u2192 controlled failure message in job record</li> <li>other exceptions \u2192 generic failure with full exception logging</li> </ul> <p>This separation helps preserve user-facing error clarity while still capturing technical diagnostics.</p>"},{"location":"backend/app/utils/#why-these-utilities-matter","title":"Why these utilities matter","text":"<ul> <li>They standardize observability across all backend modules</li> <li>They reduce duplicated serialization/exception logic</li> <li>They make asynchronous job execution easier to debug and operate</li> </ul>"},{"location":"backend/modules/pipeline/overview/","title":"Pipeline Module Overview","text":"<p>The pipeline module transforms a natural-language process description into a validated <code>PASSModel</code>.</p> <p>In contrast to single-step generation, this module executes a staged flow (<code>generate</code> \u2192 <code>validate</code> \u2192 optional <code>repair</code> \u2192 <code>validate</code>) and returns the resulting model to the job system.</p>"},{"location":"backend/modules/pipeline/overview/#integration-in-backend-runtime","title":"Integration in backend runtime","text":"<p>The pipeline is integrated through the standard backend execution stack:</p> <ol> <li>Client submits <code>POST /api/jobs</code> with <code>module: \"pipeline\"</code></li> <li>API route delegates to <code>JobService.create_job(...)</code></li> <li><code>JobService</code> resolves <code>\"pipeline\"</code> via <code>app/registry/modules.py</code></li> <li><code>modules.pipeline.main.run(payload)</code> executes in background</li> <li>Final <code>PASSModel</code> is stored in job result (<code>status: done</code>) or error is stored (<code>status: failed</code>)</li> </ol> <p>This means pipeline execution is asynchronous and follows the same job lifecycle as all other modules.</p>"},{"location":"backend/modules/pipeline/overview/#pipeline-orchestration-modulespipelinemainpy","title":"Pipeline orchestration (<code>modules/pipeline/main.py</code>)","text":"<p>The module-level <code>run(payload)</code> function is the orchestration entrypoint.</p> <p>It performs:</p> <ul> <li>input checks (<code>job_id</code>, <code>message</code>)</li> <li>model client setup (<code>ChatOpenAI</code> with payload/env overrides)</li> <li>staged execution of generate/validate/repair</li> <li>artifact logging for generation and validation outputs</li> <li>strict type check that stage outputs are <code>PASSModel</code></li> </ul> <p>Pseudo-flow:</p> <pre><code>run(payload)\n    -&gt; generate(message, model)\n    -&gt; save_artifact(prefix=\"gen\")\n    -&gt; validate(model)\n    -&gt; if issues:\n             repair(model, issues, model)\n             validate(repaired_model)\n             if issues remain: warn\n    -&gt; return PASSModel\n</code></pre> <p>Important: the current implementation performs at most one repair cycle.</p>"},{"location":"backend/modules/pipeline/overview/#inputs-and-outputs","title":"Inputs and outputs","text":""},{"location":"backend/modules/pipeline/overview/#required-payload-fields","title":"Required payload fields","text":"<ul> <li><code>message</code>: natural-language process description</li> <li><code>job_id</code>: injected by <code>JobService</code> (not normally provided by client)</li> </ul>"},{"location":"backend/modules/pipeline/overview/#optional-payload-fields","title":"Optional payload fields","text":"<ul> <li><code>model</code></li> <li><code>api_key</code></li> <li><code>base_url</code></li> <li><code>temperature</code></li> </ul>"},{"location":"backend/modules/pipeline/overview/#return-type","title":"Return type","text":"<p><code>PASSModel</code> (<code>app/models/PASSModel.py</code>) with:</p> <ul> <li><code>sid</code> (subjects + messages)</li> <li><code>sbd</code> (subject behavior diagrams)</li> </ul>"},{"location":"backend/modules/pipeline/overview/#example-job-walkthrough","title":"Example job walkthrough","text":""},{"location":"backend/modules/pipeline/overview/#1-create-job","title":"1) Create job","text":"<p>Request:</p> <pre><code>POST /api/jobs\nContent-Type: application/json\n\n{\n    \"module\": \"pipeline\",\n    \"payload\": {\n        \"message\": \"A customer places an order. The shop confirms or rejects it...\",\n        \"model\": \"gpt-5.2\",\n        \"temperature\": 0.7\n    }\n}\n</code></pre> <p>Response:</p> <pre><code>{ \"jobId\": \"6ec4e2d0-8fcb-4d7e-b8c2-8bc4c0bf3a59\" }\n</code></pre>"},{"location":"backend/modules/pipeline/overview/#2-runtime-execution","title":"2) Runtime execution","text":"<p><code>JobService</code> stores job as <code>queued</code>, then <code>running</code>, and calls the registry function for <code>pipeline</code>.</p> <p>Inside pipeline:</p> <ul> <li><code>generate</code> creates an initial structured model</li> <li><code>validate</code> returns issue list</li> <li>if issues exist, <code>repair</code> is invoked once</li> <li><code>validate</code> runs again</li> </ul> <p>Artifacts are written to <code>/app/backend/logs</code>:</p> <ul> <li><code>gen_&lt;job_id&gt;.json</code> (generation result)</li> <li><code>val_&lt;job_id&gt;.json</code> (validation issues, if any)</li> </ul>"},{"location":"backend/modules/pipeline/overview/#3-poll-job-result","title":"3) Poll job result","text":"<p>Request:</p> <pre><code>GET /api/jobs/{job_id}\n</code></pre> <p>Typical success shape:</p> <pre><code>{\n    \"jobId\": \"6ec4e2d0-8fcb-4d7e-b8c2-8bc4c0bf3a59\",\n    \"status\": \"done\",\n    \"module\": \"pipeline\",\n    \"result\": {\n        \"sid\": { \"subjects\": [...], \"messages\": [...] },\n        \"sbd\": [...]\n    },\n    \"error\": null,\n    \"created_at\": \"...\",\n    \"started_at\": \"...\",\n    \"finished_at\": \"...\"\n}\n</code></pre> <p>If generation/repair fails or output type is invalid, status becomes <code>failed</code> and <code>error</code> is populated.</p>"},{"location":"backend/modules/pipeline/overview/#operational-notes","title":"Operational notes","text":"<ul> <li>Pipeline jobs are CPU/network bound and run under <code>JobService</code> concurrency limits</li> <li>Job records are in-memory only and are lost on backend restart</li> <li>Validation warnings after repair are logged; the final returned model is still provided</li> </ul>"},{"location":"backend/modules/pipeline/stages/","title":"Pipeline Stages","text":"<p>The pipeline is implemented as three explicit stages under <code>modules/pipeline/stages</code>:</p> <ul> <li><code>generate</code></li> <li><code>validate</code></li> <li><code>repair</code></li> </ul> <p>The orchestration in <code>modules/pipeline/main.py</code> composes these stages into one module-level execution flow.</p>"},{"location":"backend/modules/pipeline/stages/#stage-1-generate-stagesgeneratemainpy","title":"Stage 1: Generate (<code>stages/generate/main.py</code>)","text":""},{"location":"backend/modules/pipeline/stages/#purpose","title":"Purpose","text":"<p>Create an initial structured <code>PASSModel</code> from natural-language input.</p>"},{"location":"backend/modules/pipeline/stages/#implementation","title":"Implementation","text":"<ul> <li>uses <code>langchain.agents.create_agent(...)</code></li> <li>configures <code>response_format=PASSModel</code> to enforce structured output</li> <li>sends generation system prompt (<code>schemes/_generationPrompt.py</code>) and user message</li> </ul>"},{"location":"backend/modules/pipeline/stages/#input-output","title":"Input / output","text":"<ul> <li>Input: <code>message: str</code>, <code>model</code> (configured <code>ChatOpenAI</code> instance)</li> <li>Output: <code>PASSModel</code></li> </ul> <p>This stage performs no semantic rule checking; it focuses on first-pass model synthesis.</p>"},{"location":"backend/modules/pipeline/stages/#stage-2-validate-stagesvalidatemainpy","title":"Stage 2: Validate (<code>stages/validate/main.py</code>)","text":""},{"location":"backend/modules/pipeline/stages/#purpose_1","title":"Purpose","text":"<p>Apply deterministic rule checks to the generated model.</p>"},{"location":"backend/modules/pipeline/stages/#output-type","title":"Output type","text":"<p>Returns <code>List[Issue]</code> where each issue contains:</p> <ul> <li><code>code</code></li> <li><code>message</code></li> <li>optional <code>path</code></li> </ul>"},{"location":"backend/modules/pipeline/stages/#validation-groups","title":"Validation groups","text":"<p>The validator aggregates issues from five rule groups:</p> <ol> <li> <p><code>_validate_sid</code>     Checks subject labels, duplicate subjects, sender/receiver consistency, message presence.</p> </li> <li> <p><code>_validate_sbds</code>     Checks SBD subject coverage, duplicate SBD subjects, duplicate states, transition source/target validity.</p> </li> <li> <p><code>_validate_transitions</code>     Enforces transition-specific constraints:</p> <ul> <li><code>DoTransition</code> must not carry message/partner</li> <li>communication transitions require <code>message</code> and <code>partner</code></li> </ul> </li> <li> <p><code>_cross_check_messages</code>     Ensures <code>SendTransition</code> / <code>ReceiveTransition</code> are declared in SID message triples.</p> </li> <li> <p><code>_validate_start_end</code>     Enforces exactly one start state per SBD and no outgoing transitions from end states.</p> </li> </ol> <p>This stage is purely rule-based and model-local (no external calls).</p>"},{"location":"backend/modules/pipeline/stages/#stage-3-repair-stagesrepairmainpy","title":"Stage 3: Repair (<code>stages/repair/main.py</code>)","text":""},{"location":"backend/modules/pipeline/stages/#purpose_2","title":"Purpose","text":"<p>Repair an invalid model using the issue list produced by validation.</p>"},{"location":"backend/modules/pipeline/stages/#implementation_1","title":"Implementation","text":"<ul> <li>builds an LLM agent with <code>response_format=PASSModel</code></li> <li>serializes current model + issue list into a JSON user message</li> <li>applies repair system prompt (<code>schemes/_repairPrompt.py</code>)</li> </ul>"},{"location":"backend/modules/pipeline/stages/#input-output_1","title":"Input / output","text":"<ul> <li>Input: <code>pass_model: PASSModel</code>, <code>issues: List[Issue]</code>, <code>model</code></li> <li>Output: repaired <code>PASSModel</code></li> </ul> <p>The contract is minimal-change repair: fix reported rule violations while preserving correct parts.</p>"},{"location":"backend/modules/pipeline/stages/#stage-composition-semantics","title":"Stage composition semantics","text":"<p><code>pipeline/main.py</code> uses these stages with the following semantics:</p> <ul> <li>generate once</li> <li>validate</li> <li>if issues exist: repair once</li> <li>validate again</li> <li>return resulting model</li> </ul> <p>If issues remain after the second validation, a warning is logged (no additional repair loop is executed in current implementation).</p>"},{"location":"backend/modules/pipeline/stages/#error-and-type-boundaries","title":"Error and type boundaries","text":"<ul> <li>Pipeline orchestration requires <code>job_id</code> and <code>message</code>, otherwise raises <code>MissingParameterException</code></li> <li>Stage outputs are checked via <code>_check_pass_model(...)</code>; invalid output raises <code>InvalidPASSModelException</code></li> <li>Exceptions propagate to <code>JobService</code>, which marks the job as <code>failed</code></li> </ul> <p>This separates deterministic validation errors (as issue list) from hard execution failures (exceptions).</p>"},{"location":"backend/modules/pipeline/stages/#example-stage-level-trace","title":"Example stage-level trace","text":"<p>For a single pipeline job, a typical trace is:</p> <ol> <li><code>generate</code> returns initial model</li> <li><code>validate</code> returns e.g. <code>SEND_NOT_IN_SID</code>, <code>START_STATE_COUNT</code></li> <li><code>repair</code> receives original model + issues and returns updated model</li> <li><code>validate</code> reruns; issues list becomes empty</li> <li>pipeline returns repaired <code>PASSModel</code></li> </ol> <p>If step 4 still returns issues, job may still complete with <code>done</code>, but unresolved issues are logged as warnings.</p>"},{"location":"config/overview/","title":"Configuration Overview","text":"<p>This project separates runtime configuration into environment files under <code>config/</code>.</p>"},{"location":"config/overview/#frontend-config","title":"Frontend config","text":"<p>The frontend uses <code>config/frontend.env</code>.</p> <p>Most important variable:</p> <ul> <li><code>BACKEND_URL</code> \u2192 backend base URL used by Next.js API proxy routes (<code>frontend/app/api/jobs/*</code>)</li> </ul> <p>Example (<code>config/frontend.env.example</code>):</p> <pre><code>BACKEND_URL=http://backend:8000\n</code></pre> <p>With Docker Compose, <code>http://backend:8000</code> targets the backend service name on the internal network.</p>"},{"location":"config/overview/#backend-config","title":"Backend config","text":"<p>The backend reads its own variables from <code>config/backend.env</code> (via <code>docker-compose.yaml</code>).</p> <p>Most important variable:</p> <ul> <li><code>API_KEY</code> \u2192 api key used for llm requests. Make sure to provide the correct key for the selected model (<code>frontend/app/api/jobs/*</code>)</li> </ul> <p>Example (<code>config/frontend.env.example</code>):</p> <pre><code>API_KEY=sk-proj-oJ...\n</code></pre> <p>Keep both real env files (<code>backend.env</code>, <code>frontend.env</code>) out of version control and maintain defaults in <code>*.env.example</code>.</p>"},{"location":"frontend/modules/","title":"Frontend Modules","text":"<p>Frontend functionality is implemented as reusable module cards under <code>frontend/app/modules</code>.</p> <p>All cards share the same visual shell (<code>ModuleCardSpec</code>) and are orchestrated by <code>app/page.tsx</code>.</p>"},{"location":"frontend/modules/#shared-card-shell-modulecardspec","title":"Shared card shell: <code>ModuleCardSpec</code>","text":"<p><code>ModuleCardSpec</code> provides a consistent container for:</p> <ul> <li>title and description</li> <li>card body content</li> <li>optional footer area</li> </ul> <p>This keeps style/layout consistency while allowing module-specific logic inside each card.</p>"},{"location":"frontend/modules/#module-cards","title":"Module cards","text":""},{"location":"frontend/modules/#standardmodulecard","title":"<code>StandardModuleCard</code>","text":"<p>Used for direct message-based modules (<code>cot</code>, <code>pipeline</code>).</p> <p>Behavior:</p> <ul> <li>collects free-text input</li> <li>queues job via <code>useJobRunner().run(module, { message })</code></li> <li>notifies parent to refresh jobs after queueing</li> </ul>"},{"location":"frontend/modules/#refinemodulecard","title":"<code>RefineModuleCard</code>","text":"<p>Used for refinement workflow.</p> <p>Behavior:</p> <ul> <li>uses selected source job from sidebar</li> <li>fetches source job details (<code>/api/jobs/{sourceJobId}</code>)</li> <li>queues <code>refine</code> job with <code>{ source_job_id, message, result }</code></li> </ul>"},{"location":"frontend/modules/#exportmodulecard","title":"<code>ExportModuleCard</code>","text":"<p>Used to export a finished source job to <code>.json</code>, <code>.txt</code>, or <code>.owl</code>.</p> <p>Behavior:</p> <ul> <li>selects export format</li> <li>queues <code>export</code> job with <code>{ source_job_id, format, result }</code></li> <li>watches export job in job list</li> <li>creates downloadable <code>Blob</code> from returned base64 payload</li> </ul>"},{"location":"frontend/modules/#resultmodulecard","title":"<code>ResultModuleCard</code>","text":"<p>Used to inspect a specific job result.</p> <p>Behavior:</p> <ul> <li>loads selected/pasted job via <code>/api/jobs/{id}</code></li> <li>shows status/errors for non-finished jobs</li> <li>renders result through <code>renderResult(...)</code></li> </ul>"},{"location":"frontend/modules/#jobssidebar","title":"<code>JobsSidebar</code>","text":"<p>Operational control panel for all jobs.</p> <p>Behavior:</p> <ul> <li>lists jobs sorted by latest update timestamp</li> <li>displays status badges (<code>queued</code>, <code>running</code>, <code>done</code>, <code>failed</code>)</li> <li>starts/stops polling for active jobs</li> <li>emits selected job id to parent</li> </ul>"},{"location":"frontend/modules/#type-contracts","title":"Type contracts","text":"<p><code>app/types.ts</code> defines the shared frontend model:</p> <ul> <li><code>JobStatus</code></li> <li><code>Job</code></li> <li><code>ExportJobResult</code></li> <li><code>ExportFormat</code></li> </ul> <p>These types align UI assumptions with backend response shapes and reduce ad-hoc parsing in components.</p>"},{"location":"frontend/overview/","title":"Frontend Overview","text":"<p>The frontend is a Next.js App Router application (<code>frontend/app</code>) that provides a unified UI for triggering backend modules and inspecting asynchronous job results.</p> <p>Its core design principle is: keep browser code simple by routing all backend communication through internal Next.js API routes.</p>"},{"location":"frontend/overview/#architecture-at-a-glance","title":"Architecture at a glance","text":"<p>The frontend is organized into three main parts:</p> <ul> <li>UI composition in <code>app/page.tsx</code> (dashboard layout + module cards)</li> <li>API proxy layer in <code>app/api/jobs/*</code> (server-side forwarding to backend)</li> <li>Reusable utilities in <code>app/util/*</code> (job execution/polling and rendering helpers)</li> </ul> <p>This gives a clean separation between view logic, network boundaries, and shared helper behavior.</p>"},{"location":"frontend/overview/#runtime-interaction-with-backend","title":"Runtime interaction with backend","text":"<p>The browser never calls the backend host directly.</p> <p>Instead, UI components call local routes:</p> <ul> <li><code>POST /api/jobs</code></li> <li><code>GET /api/jobs</code></li> <li><code>GET /api/jobs/{id}</code></li> </ul> <p>Those Next.js route handlers forward requests to <code>BACKEND_URL</code> (from environment config), preserving response status/body.</p> <p>Benefits:</p> <ul> <li>stable same-origin API surface for the client</li> <li>no backend URL hardcoding in browser components</li> <li>deployment flexibility via environment variables</li> </ul>"},{"location":"frontend/overview/#main-user-flow","title":"Main user flow","text":"<ol> <li>User enters module input in one of the cards</li> <li>Card queues a job through <code>useJobRunner().run(...)</code></li> <li>Jobs list refreshes and shows status (<code>queued</code>/<code>running</code>/<code>done</code>/<code>failed</code>)</li> <li>Sidebar polling updates active jobs periodically</li> <li>Result and export cards consume selected job data</li> </ol> <p>The page acts as a module dashboard over a shared asynchronous job queue.</p>"},{"location":"frontend/overview/#technology-summary","title":"Technology summary","text":"<ul> <li>Next.js 16 App Router</li> <li>React 19 client components for interactive cards/sidebar</li> <li>TypeScript types in <code>app/types.ts</code></li> <li>Tailwind-based styling in <code>app/globals.css</code></li> </ul>"},{"location":"frontend/overview/#design-trade-offs","title":"Design trade-offs","text":"<ul> <li>Strength: low coupling between UI and backend host via proxy routes</li> <li>Strength: modular card-based feature extension in <code>app/modules</code></li> <li>Trade-off: polling-based updates (simple/reliable) instead of real-time push</li> <li>Trade-off: single-page dashboard pattern; all modules share the same screen context</li> </ul>"},{"location":"frontend/routing-layout/","title":"Frontend Routing &amp; Layout","text":"<p>The frontend uses the Next.js App Router with a compact route surface.</p>"},{"location":"frontend/routing-layout/#route-structure","title":"Route structure","text":""},{"location":"frontend/routing-layout/#ui-routes","title":"UI routes","text":"<ul> <li><code>/</code> \u2192 main dashboard (<code>app/page.tsx</code>)</li> </ul>"},{"location":"frontend/routing-layout/#internal-api-routes-proxy","title":"Internal API routes (proxy)","text":"<ul> <li><code>/api/jobs</code> (<code>app/api/jobs/route.ts</code>)<ul> <li><code>POST</code>: create backend jobs</li> <li><code>GET</code>: list jobs</li> </ul> </li> <li><code>/api/jobs/[id]</code> (<code>app/api/jobs/[id]/route.ts</code>)<ul> <li><code>GET</code>: fetch single job details</li> </ul> </li> </ul> <p>These API routes are consumed by client components and forward to backend endpoints under <code>${BACKEND_URL}/api/jobs...</code>.</p>"},{"location":"frontend/routing-layout/#layout-composition","title":"Layout composition","text":""},{"location":"frontend/routing-layout/#root-layout-applayouttsx","title":"Root layout (<code>app/layout.tsx</code>)","text":"<p>The root layout defines global HTML/body structure, metadata, and font setup (<code>Geist</code>, <code>Geist Mono</code>).</p>"},{"location":"frontend/routing-layout/#dashboard-page-apppagetsx","title":"Dashboard page (<code>app/page.tsx</code>)","text":"<p>The main page composes a two-part layout:</p> <ul> <li>left sidebar: <code>JobsSidebar</code> (job list, polling-driven status updates)</li> <li>content grid: module cards (<code>StandardModuleCard</code>, <code>RefineModuleCard</code>, <code>ExportModuleCard</code>, <code>ResultModuleCard</code>)</li> </ul> <p>On larger viewports, the sidebar is fixed and the module area offsets with a left margin. On smaller viewports, sidebar/content stack vertically.</p>"},{"location":"frontend/routing-layout/#state-ownership-on-the-page","title":"State ownership on the page","text":"<p><code>app/page.tsx</code> is the state owner for:</p> <ul> <li><code>jobs</code></li> <li>loading/error state for job list</li> <li>currently selected job id</li> </ul> <p>This state is passed down into sidebar/cards via props, which keeps inter-module coordination explicit and predictable.</p>"},{"location":"frontend/routing-layout/#navigation-model","title":"Navigation model","text":"<p>The current UI uses in-page module switching (card interactions + selected job state), not multi-page navigation.</p> <p>That keeps workflow friction low for rapid iteration:</p> <ul> <li>queue a module run</li> <li>watch status in sidebar</li> <li>inspect/refine/export from the same screen</li> </ul>"},{"location":"frontend/util/polling/","title":"Polling Utility","text":"<p>Job polling is implemented in <code>frontend/app/util/jobRunner.tsx</code> via the <code>useJobRunner()</code> hook.</p> <p>The hook combines job creation and incremental status tracking for asynchronous backend jobs.</p>"},{"location":"frontend/util/polling/#hook-api","title":"Hook API","text":"<p><code>useJobRunner()</code> exposes:</p> <ul> <li><code>loading</code>, <code>jobId</code></li> <li><code>run(module, payload)</code></li> <li><code>setPollIds(ids)</code></li> <li><code>addPollIds(ids)</code></li> <li><code>startPolling(onUpdate, intervalMs)</code></li> <li><code>stopPolling()</code></li> </ul>"},{"location":"frontend/util/polling/#execution-responsibilities","title":"Execution responsibilities","text":""},{"location":"frontend/util/polling/#job-creation","title":"Job creation","text":"<p><code>run(module, payload)</code>:</p> <ul> <li>sends <code>POST /api/jobs</code></li> <li>extracts and stores returned <code>jobId</code></li> <li>prevents duplicate submits while loading</li> </ul>"},{"location":"frontend/util/polling/#poll-lifecycle","title":"Poll lifecycle","text":"<p><code>startPolling(...)</code> performs periodic <code>GET /api/jobs/{id}</code> requests for tracked ids.</p> <p>It:</p> <ul> <li>updates each job through callback (<code>onUpdate(job)</code>)</li> <li>removes terminal jobs from tracked set (<code>done</code>, <code>failed</code>)</li> <li>auto-stops when no active ids remain</li> <li>retries with backoff-like delay handling on polling errors</li> </ul>"},{"location":"frontend/util/polling/#cleanup","title":"Cleanup","text":"<p>Hook cleanup and <code>stopPolling()</code> clear pending timers to prevent stale background polling.</p>"},{"location":"frontend/util/polling/#integration-point","title":"Integration point","text":"<p><code>JobsSidebar</code> owns polling orchestration in the UI:</p> <ul> <li>derives active ids from current <code>jobs</code> list (<code>queued</code>/<code>running</code>)</li> <li>calls <code>setPollIds(activeIds)</code></li> <li>starts polling when needed, stops when no active jobs remain</li> </ul> <p>This keeps polling policy centralized in one component while the hook handles mechanics.</p>"},{"location":"frontend/util/polling/#why-polling-is-used","title":"Why polling is used","text":"<ul> <li>matches asynchronous backend job model well</li> <li>simple to operate and debug</li> <li>avoids additional websocket/SSE infrastructure</li> </ul> <p>The current strategy favors implementation simplicity over real-time push complexity.</p>"}]}